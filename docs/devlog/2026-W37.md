# 2026 W37 (09-08 ~ 09-14)

## 09-13 (일)
- 앱 프로젝트 시작. 아이디어 6개 평가 → A(ISA Lab 앱) 먼저, DIY 수리 앱 다음
- RN(Expo) 결정 (ADR-0001) · 엔진을 워크스페이스 패키지로 (ADR-0002) · 서버 없음 (ADR-0003)
- `apps/mobile` 골격 + 화면 3개 첫 구현 · `npm test` 59 통과 · 브랜치 `feat/mobile`
- ponytail 플러그인을 프로젝트 스코프로 설치 — 데스크톱 앱엔 `/plugin`이 없어 CLI로
- D2 입력·결과 화면이 Expo Go에서 동작. 버그: 자산군마다 `이익 − 손실`을 한 줄로 넘겨 해외·채권 ETF 손실이 일반계좌에서도 통산됨 → 예시 절세액 308,000원 → 이익·손실 두 행으로 넘겨 1,540,000원 (ADR-0004)
- D3 근거 화면 · 설정 시트 · AsyncStorage 저장 · 「예시로 보기」는 채운 뒤 결과로 바로 · 설정 진입점은 헤더 「설정」 하나로. 폰에서 앱을 껐다 켜도 설정 유지 확인
- Xcode 설치 → iOS 시뮬레이터에서 Expo Go 실행. 데스크톱 앱 시뮬레이터 패널이 까맣게 나와 앱 재시작으로 해결
- 다음: 스토어에 올리기 직전 단계까지 다듬기 (아이콘 · 심사 대비)

## 09-14 (월)
- 출시 준비: 아이콘·적응형 아이콘·스플래시를 코드로 그림(`make-icons.swift`) · 접근성 라벨 · 웹 `/privacy` 개인정보처리방침 · iOS 개인정보 매니페스트 · `eas.json` · 스토어 문구·스크린샷(`docs/store`)
- 세법 바로잡음: ISA 안 국내 상장주식 순손실 차감(조특령 제93조의4⑨) · 시행되지 않은 2024 ISA 개편안 룰셋 삭제(ADR-0006) · 출처를 법령 원문 링크로
- 릴리스 빌드 확인: iOS 시뮬레이터 · Android Studio 설치 후 Android 16 에뮬레이터(뒤로가기·키보드·적응형 아이콘). Android는 Android Studio 내장 JDK 25로 CMake 단계가 실패해 JDK 17로 빌드
- 웹 세율 숫자를 룰셋으로(배지·용어 사전·안내 문구). 금융소득종합과세 경고가 딱 2,000만원에도 「넘었다」던 경계 버그 수정 — 2,000만원 「이하」는 분리과세(소득세법 제14조③6호)
- PR #1 최종 리뷰에서 나온 것: ISA 의무보유를 개월 수로 세서 최대 30일 일찍 「충족」 → 날짜로 비교 · 쉼표 소수점 입력 · 앱 LICENSE가 Expo 템플릿 그대로 · README 대시보드 스크린샷 재촬영
- PR #1 병합 → Vercel 운영에 `/privacy` 공개 · `feat/mobile` 삭제 · 안 쓰는 스크린샷 2장 삭제 · README에 개발 도구·확인한 버전
- 이슈 #2 `npm run lint` 고장(eslint 미설치, 첫 커밋부터) · #3 12월 정기국회 뒤 가상자산 과세 룰셋 재확인(2029·2030년 유예 법안 계류)
- GitHub Actions 추가 — push·PR마다 `npm ci` → `npm test`. 테스트 59 → 75
- 덜내 Android 미리보기 APK — EAS Build `preview`로 빌드(0.1.0, 66 MB, 서명 키는 EAS 보관). EAS 링크는 14일 뒤 만료라 GitHub 릴리스 `deolnae-v0.1.0`에 올리고 README에 다운로드 링크·설치 안내
- 다음: 폰에 APK 설치 확인 · #2 · #3(12월) · 스토어에 올릴 때 Apple·Google 개발자 계정 → 스토어 빌드·제출
